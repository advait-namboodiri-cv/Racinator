export default function TracksPage() {
    return (
      <main className="min-h-screen bg-neutral-950 text-white px-6 py-10">
        <h1 className="text-3xl font-bold text-red-500">Tracks</h1>
        <p className="mt-4 text-neutral-300">
          Track map explorer that visualizes circuit layouts, GPS traces, and key features like corners and sectors.
        </p>
      </main>
    );
  }